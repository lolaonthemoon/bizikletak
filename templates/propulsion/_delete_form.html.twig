<form method="post" action="{{ path('propulsion_delete', {'id': propulsion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ propulsion.id) }}">
    <button class="btn">Delete</button>
</form>
